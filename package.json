{
  "dependencies": {
    "@hubspot/cli": "^3.0.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "upload": "npx hs upload surfing-theme surfing-theme",
    "watch:dev": "hs watch --portal=hubspot-sandbox surfing-theme surfing-theme --initial-upload --remove"
  }
}
